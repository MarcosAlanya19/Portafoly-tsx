import { SectionPage } from '../atoms';
import { ListSkills } from '../molecules';

export const Skills = () => {
  return (
    <SectionPage subtitle='Habilidades' classname='h-screen'>
      <ListSkills />
    </SectionPage>
  );
};
